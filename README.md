## Webpack starter

To get started, run

```sh
$ npm install -g gulp
$ npm install
$ gulp
```

The following gulp tasks are available.

```
├── tdd
├── test
├─┬ build
│ └── webpack:build
├─┬ build-dev
│ └── webpack:build-dev
├── webpack:build
├── webpack:build-dev
└─┬ default
  └── tdd
```
